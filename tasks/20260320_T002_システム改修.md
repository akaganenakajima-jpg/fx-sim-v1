# T002: システム改修タスク（総合評価レポートより）

## 背景
IPA知識ベース（SA/SM/DB/AU/セキュリティ）によるシステム総合評価（スコア85.9/100）から抽出した改善項目。

## 改修項目

### 🔴 優先度: 高
- [ ] **D1インデックス追加** — データ増加前に予防的に追加
  ```sql
  CREATE INDEX idx_positions_status ON positions(status);
  CREATE INDEX idx_positions_pair_status ON positions(pair, status);
  CREATE INDEX idx_decisions_pair_created ON decisions(pair, created_at DESC);
  CREATE INDEX idx_system_logs_id_desc ON system_logs(id DESC);
  ```

### 🟡 優先度: 中
- [ ] **TP/SLレートのサニティチェック** — AIが極端な値を返した場合の防御（例: SL距離 > 現在値の5%を拒否）
- [ ] **マイグレーション管理の体系化** — `schema_v2_migrated` フラグ方式 → `schema_version` テーブルに移行
- [ ] **`index.ts` の `run()` 関数分割** — 810行モノリシック → `fetchMarketData()` / `runAIDecisions()` / `runDailyTasks()` に分割

### 🟢 優先度: 低
- [ ] **外部通知（Slack/Discord Webhook）** — DRAWDOWN・ERROR発生時に即時通知（Phase 3で計画済み）
- [ ] **Yahoo Finance代替プロバイダー追加** — Twelve Data API等をバックアップに
- [ ] **APIエンドポイントのレート制限** — DoS防止

### 🔒 実弾移行時に実施
- [ ] **`/api/status` に認証追加** — Bearer トークンまたは Cloudflare Access で保護
- [ ] **CORS設定の明示化**

## 評価サマリー
| カテゴリ | スコア |
|----------|--------|
| 可用性 | A (90) |
| 性能 | A- (87) |
| 運用性 | A (90) |
| 移行性 | A (88) |
| セキュリティ | B+ (78) |
| 環境適合性 | A (92) |
| DB設計 | B+ (80) |
| **総合** | **85.9 / 100** |
