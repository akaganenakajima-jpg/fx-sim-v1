# T023: Workers AI センチメント分析（ハイブリッドAIアーキテクチャ）実装

## 背景・目的

ニュース7軸スコアリングのs軸（sentiment = シグナル強度）を、
汎用LLM（Gemini）から Cloudflare Workers AI の特化モデル（`@cf/huggingface/distilbert-sst-2-int8`）に切り出す。

- Geminiの負荷軽減
- エッジ推論による高速化・精度向上
- Workers AI失敗時はGeminiのs値にフォールバック（ゼロダウンタイム）

**プランファイル**: `~/.claude/plans/fluffy-soaring-cookie.md`（詳細設計あり）

## 実装ステップ（プランファイルより）

- [ ] Step 1: `wrangler.toml` に `[ai]` binding追加
- [ ] Step 2: `src/index.ts` — Env型にAI追加 + filterAndTranslateNews引数追加
- [ ] Step 3: `src/ai-sentiment.ts` — 新規ファイル（Workers AI DistilBERT-SST2 バッチ処理）
- [ ] Step 4: `src/news.ts` — ハイブリッド統合（シグネチャ拡張・Workers AI並列実行・s値マージ）
- [ ] Step 5: `src/api.ts` — SELECTにscoresカラム追加
- [ ] Step 6: `src/app.js.ts` — ⚡ Edge AI バッジ描画
- [ ] Step 7: `src/style.css.ts` — `.nf-wai-badge` CSS追加
- [ ] Step 8: `src/constants.ts` + `src/dashboard.ts` — キャッシュバスト

## 次のアクション

1. プランファイル `fluffy-soaring-cookie.md` を読んで詳細設計を確認
2. feature ブランチを切って実装開始（例: `feature/20260405-workers-ai-sentiment`）
3. `npx tsc --noEmit` → `npm test` → デプロイ → スクショ確認

## 作成日

2026-04-05
