# T005: セッション引き継ぎ — 2026-03-21

## このセッションで完了したこと

### 1. ダッシュボード UX リニューアル（feature/20260321-batch-c-statistics → master #6）

**CSS（style.css.ts）**
- `--radius: 12px → 16px`、`--radius-sm: 8px → 10px`
- ヘッダー glassmorphism: `background: rgba(28,28,30,0.92)` + `backdrop-filter: blur(20px)`
- タブバー glassmorphism + ライトモード opacity 統一 (0.92)
- 新コンポーネント追加: `.market-state-bar`、`.ai-ranking-*`、`.power-progress-*`、`.regime-badge`、`.stats-narrative-*`、`.ci-bar-*`

**HTML（dashboard.ts）**
- ヒーロー内に `#power-progress-wrap`（統計的有意性プログレスバー）
- `#market-state-bar`（市場状態 4列グリッド）
- `#ai-ranking-section`（AI期待銘柄ランキング）
- 統計タブを `<div id="stats-narrative">` に刷新

**JS（app.js.ts）**
- `calcRegime()` + `regimeBadgeHtml()` — EWMA×ボラティリティから trending/ranging/volatile 判定
- `renderMarketStateBar()` — レジーム・信頼区間・勝率表示
- `renderAiRanking()` — 階層ベイズ勝率上位3銘柄
- `renderPowerProgress()` — 検出力達成率バー
- `renderPerfSummary()` 全面刷新 — ナラティブ型（問い→答え）3セクション構造

### 2. UI 細部修正（master 直コミット）

| 修正 | 内容 |
|---|---|
| ヘッダー余白 | `padding-top: 58px → 74px`（ヘッダー重なり解消） |
| 絵文字全廃 | 🥇🥈🥉 → 数字丸バッジ、📊⚖️🎯 → テキスト、TP/SLバナー → SVG |
| SELL通知隙間 | `card-watchlist padding: 12px 0 → 0`、`ai-ranking-list margin-bottom: 8px → 0` |
| flex 二重スペーシング | gap と margin-bottom の加算を解消 |

### 3. ドキュメント更新

- `docs/06_デザイン_ブランドガイドライン.md`
  - Section 4.2: content padding-top 計算式追記
  - Section 10.7: 絵文字禁止を例外なしに強化（ランキングも数字バッジ）
  - Section 10.11（新規）: flex 二重スペーシング禁止ルール
  - チェックリスト 12項目に拡充

---

## 現在の状態

### Git
- ブランチ: `master`（clean）
- 最新コミット: `52d6132 docs: ブランドガイドライン更新`
- リモート: 同期済み

### 本番
- URL: `https://fx-sim-v1.ai-battle-sim.workers.dev`
- デプロイ済み Version: `a71023c0-604b-4e02-867e-cea4f11879f8`
- 稼働状況: cron 正常・301件取引記録済み・ROI +6.20%

### T004 進捗
- **19/22 完了（86.4%）**
- 残: T004-15（Yahoo Finance代替、2h）、T004-20〜22（実弾移行時セキュリティ）

---

## 次のセッションでやること

### 優先度順

1. **OANDA 口座認証確認**（認証メール届いていれば Step 9 E2Eテストへ）
   - デモ口座で発注テスト
   - RiskGuard 動作確認
   - 実弾フラグ切り替え手順確認

2. **T004-15: Yahoo Finance 代替プロバイダー**（Twelve Data API、2h）
   - `indicators.ts` にフォールバック追加
   - 障害時自動切り替え

3. **パフォーマンスチューニング**
   - 実行時間 33-53秒台の WARN → 原因特定と並列化改善
   - Sharpe比 0.01 改善 → RR比チューニング（TP距離を広げる方向）

4. **T004-20〜22**（実弾移行決定後）
   - `/api/status` 認証
   - CORS 明示化
   - レート制限

---

## 注意事項

- OANDA 認証は口座開設時のメールに記載された URL から完了する
- 実弾移行前に必ず RiskGuard パラメータ（`maxDailyLoss`, `maxLot`）を実弾用に調整すること
- `src/style.css.ts` の `.tp-banner-icon` は `innerHTML` で SVG を差し込む設計に変更済み（`textContent` 不可）
